[scenario]
name         = SIDFX stereo (D400=SIDFX + second SID)
detect_wait  = 9

[expect]
; SIDFX sits at D400 and provides a second SID at an address determined
; by the SIDFX jumper configuration (typically D500 or D420).
; Fill in slot2_addr and slot2_type after measuring on real hardware.

slot1_addr   = D400
slot1_type   = 30       ; SIDFX

; slot2_addr = D500     ; uncomment and set after hardware measurement
; slot2_type = ??       ; 01=6581 02=8580 or other
