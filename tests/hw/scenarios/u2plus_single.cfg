[scenario]
name         = Ultimate II+ single SID (D400 only)
detect_wait  = 9

[expect]
; U2+ in single SID mode with virtual SID enabled.
; Reports as FPGASID since U2+ uses the same FPGA SID core.
; Only one slot populated.

slot1_addr   = D400
slot1_type   = 06       ; FPGASID 8580 -- change to 07 if configured as 6581
