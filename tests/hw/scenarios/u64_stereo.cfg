[scenario]
name         = Ultimate 64 stereo (D400 + D420, both FPGASID)
detect_wait  = 9

[expect]
; U64 built-in SID uses the FPGASID core.
; Stereo address is configurable in U64 settings (default D420).
; Both slots will report FPGASID type ($06 or $07 depending on configured model).
; Adjust slot types to match your U64 SID model configuration.

slot1_addr   = D400
slot1_type   = 06       ; FPGASID 8580 -- change to 07 if configured as 6581
slot2_addr   = D420
slot2_type   = 06       ; FPGASID 8580 -- change to 07 if configured as 6581
